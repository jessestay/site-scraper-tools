// Shared configuration between free and premium versions
const CONFIG = {
  version: '1.0.4',
  features: {
    rateLimit: true,
    memoryManagement: true,
    assetCaching: true
  }
};

window.SCRAPER_CONFIG = CONFIG; 